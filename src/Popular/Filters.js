import React, { Component } from 'react';
import styled from 'styled-components';
import media from '../common/media';

class Filters extends Component {
  render() {
    return <div>фильтры</div>;
  }
}

export default Filters;
